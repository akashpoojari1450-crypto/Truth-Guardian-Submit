[project]
name = "truth-guardian-vak"
version = "1.0.0"
description = "Truth Guardian Scam Detector"
requires-python = ">=3.10"
dependencies = [
    "openenv-core>=0.2.0",
    "fastapi",
    "uvicorn",
]

[project.scripts]
serve = "inference:run_inference"
